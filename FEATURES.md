# FEATURES — 기능 설명 (v1.0.0)

## 1. 메뉴판
- Very Vary 실제 메뉴판을 벡터(텍스트+SVG)로 재구성해서 표시. 화질 걱정 없이 항상 선명합니다.
- Coffee / Latte&Tea / Ade&Smoothie / Blend Tea / TWG Tea 카테고리별 카드로 정리.

## 2. 날짜 · 시각
- 상단에 오늘 날짜(요일 포함)와 현재 시각(HH:MM:SS)이 큼직하게 표시되고, 시각은 1초마다 갱신됩니다.

## 3. 탭 구조 (메뉴 주문 / 오늘의 주문 현황 / 누적 주문 통계)
- 상단 탭 버튼으로 전환하거나, 화면을 좌우로 스와이프(드래그)하면 슬라이드 애니메이션과 함께 옆 탭으로 이동합니다.

## 4. 메뉴 주문 탭
- 멤버 12명(고정 순서) 각각 이름 옆 드롭다운에서 메뉴(Hot/Ice 구분)를 선택합니다.
- 메뉴 선택 시 카드가 아래로 늘어나며, 옵션을 해시태그 칩 형태로 선택할 수 있습니다.
  - 디카페인으로 변경 / 1샷 추가·2샷 추가(택1 또는 미선택) / 우유를 오트밀크로 변경 /
    바닐라 시럽 추가·헤이즐넛 시럽 추가(택1 또는 미선택)
- **저장** 버튼: 선택한 메뉴를 확정 저장하고, 드롭다운·옵션을 잠근 뒤 **수정** 버튼을 활성화합니다.
- **수정** 버튼: 저장된 내용을 다시 바꿀 수 있게 잠금을 풉니다.
- **추천 배지**: 해당 멤버의 확정된 누적 주문 중 75% 이상을 차지하는 메뉴가 있으면
  드롭다운 위에 "추천: 메뉴명"으로 표시되고, 클릭하면 그 메뉴가 자동 선택됩니다.
- **주문 마감** 버튼: 그날 주문을 확정합니다. 누르고 나면 모든 멤버의 저장/수정이
  잠기고, 그날 주문이 비로소 "확정"으로 표시되며 누적 통계에 반영됩니다.

## 5. 오늘의 주문 현황 탭
- 그날 저장된 주문을 메뉴(Hot/Ice) → 옵션 순으로 그룹핑해서, 같은 메뉴를 시킨 멤버를
  팀 순서대로 나열하고 "(N명)"으로 인원수를 표시합니다.
- 상단에 (미확정)/(확정) 상태 배지가 표시되고, 맨 아래에 "총 00명"으로 합계가 나옵니다.

## 6. 멤버별 누적 주문 통계 탭
- **주문 마감으로 확정된 날짜만** 집계에 반영됩니다 (미확정 상태의 오늘 주문은 제외).
- 과거 4건의 이력(Loop 단락 / 0624 커피 / 0902 산뜻한 아침의 커피 주문 / 뭘닝 커피)은
  이미 지나간 확정 주문으로 간주해 항상 집계에 포함됩니다.
- 멤버별로 메뉴를 주문 빈도 내림차순으로 정리해서 보여줍니다.

## 7. 디자인
- 복숭아·라벤더·민트 파스텔 그라데이션 배경이 전체 페이지에 이어져 있고, 섹션은
  얇은 구분선으로 나뉩니다.
- 저장 버튼은 코랄 핑크, 수정·옵션 하이라이트는 파스텔 블루→라벤더(노을 하늘 톤)
  그라데이션으로 구분됩니다.
- 드롭다운은 커스텀 화살표 아이콘과 모던한 타이포로 스타일링되어 있습니다.
- 좁은 화면(모바일)에서도 레이아웃이 자연스럽게 줄바꿈되도록 반응형으로 구성했습니다.

## 8. 데이터 저장 방식 (이 Vercel 버전)
- claude.ai 버전은 Claude의 실시간 공유 DB를 사용했지만, 이 버전은 `api/data.js`
  서버리스 함수 + Upstash Redis를 통해 데이터를 저장하고, 프론트엔드는 4초마다
  서버에 최신 상태를 물어보는 폴링 방식으로 동기화합니다.
- 배포 후 첫 API 호출 시 과거 4건의 이력 데이터가 자동으로 한 번만 시딩됩니다.
